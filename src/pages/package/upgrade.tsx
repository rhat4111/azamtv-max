import { default as UpgradePackageContainer } from "../../containers/package/upgrade/upgrade";
import Layout from "../../components/layout/layout/layout";

const UpgradePackage = () => {
  return <Layout><UpgradePackageContainer /></Layout>;
};

export default UpgradePackage;
