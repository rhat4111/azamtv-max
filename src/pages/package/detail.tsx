import { default as DetailPackageContainer } from "../../containers/package/detail/detail";
import Layout from "../../components/layout/layout/layout";

const DetailPackage = () => {
  return <Layout><DetailPackageContainer /></Layout>;
};

export default DetailPackage;
