import { default as PlayerContainer } from "../containers/player/player";
import Layout from "../components/layout/layout/layout";

const Player = () => {
  return (
    <Layout>
      <PlayerContainer />
    </Layout>
  );
};

export default Player;
