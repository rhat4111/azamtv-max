import { default as ProfileContainer } from "../../containers/profile/profile/profile";
import Layout from "../../components/layout/layout/layout";

const Profile = () => {
  return <Layout><ProfileContainer /></Layout>;
};

export default Profile;
