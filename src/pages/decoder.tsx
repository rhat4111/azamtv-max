import { default as DecoderContainer } from "../containers/decoder/decoder";
import Layout from "../components/layout/layout/layout";

const Decoder = () => {
  return (
    <Layout>
      <DecoderContainer />
    </Layout>
  );
};

export default Decoder;
