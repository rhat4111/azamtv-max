import { default as TermsContainer } from "../containers/terms/terms";
import Layout from "../components/layout/layout/layout";

const Terms = () => {
  return <Layout><TermsContainer /></Layout>;
};

export default Terms;
