import { default as TransactionContainer } from "../containers/transaction/transaction";
import Layout from "../components/layout/layout/layout";

const Transaction = () => {
  return (
    <Layout>
      <TransactionContainer />
    </Layout>
  );
};

export default Transaction;
