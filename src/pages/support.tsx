import { default as SupportContainer } from "../containers/support/support";
import Layout from "../components/layout/layout/layout";

const Support = () => {
  return <Layout><SupportContainer /></Layout>;
};

export default Support;
